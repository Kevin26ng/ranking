
/*import axios from "axios";

const API_URL = "http://localhost:5000/api";

export const getDebaters = async () => {
  try {
    const response = await axios.get(`${API_URL}/debaters`);
    return response.data; // return debaters data
  } catch (error) {
    console.error("Error fetching debaters:", error);
    throw error;
  }
};

// POST a new debater
export const addDebater = async (newDebater) => {
  try {
    const response = await axios.post(`${API_URL}/debaters`, newDebater);
    return response.data; // return the created debater
  } catch (error) {
    console.error("Error adding debater:", error);
    throw error;
  }
}; */
